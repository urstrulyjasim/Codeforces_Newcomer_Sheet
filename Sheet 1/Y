#include<stdio.h>
int main()
{
    long long a,b,c,d,N,temp;
    scanf("%lld %lld %lld %lld",&a,&b,&c,&d);
    temp=((a%100)*(b%100)*(c%100)*(d%100))%100;

    if(temp<10){
        printf("0%lld\n",temp);
    }
    else
    printf("%lld\n",temp);

    return 0;
}
