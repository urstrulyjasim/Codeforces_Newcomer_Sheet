#include<stdio.h>
int main()
{
    double x;
    scanf("%lf",&x);
    int y = x;
    if(x-y>0){
        printf("float %d %f\n",y,x-y);
    }
    else if((x-y)==0){
        printf("int %d\n",y);
    }


    return 0;
}
