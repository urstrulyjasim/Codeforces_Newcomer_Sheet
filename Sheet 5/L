#include<stdio.h>
#include<stdlib.h>
#define s 100001
 
void print(int arr[s], int size)
{
   for(int i=0; i<size; i++)
      printf("%d ", arr[i]);
}
 
 
int main()
{
   int n;
   scanf("%d", &n);
   int arr1[s];
   int arr2[s];
   for(int i=0; i<n; i++)
      scanf("%d", &arr1[i]);
   for(int i=0; i<n; i++)
      scanf("%d", &arr2[i]);
      
   print(arr2, n);
   print(arr1, n);
      
   
   return 0;
}
