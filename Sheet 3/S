#include<stdio.h>
int main()
{
    int n,m,i,j,t,count=0;
    scanf("%d %d",&n,&m);
    int a[n][m];
    for( i=0;i<n;i++)
    {
        for( j=0;j<m;j++)
        {
            scanf("%d",&a[i][j]);
        }
    }
    scanf("%d",&t);
    for(i=0;i<n;i++)
    {
        for(j=0;j<m;j++)
        {
            if(t==a[i][j])
            {
                count++;
            }
        }
    }
    if(count==0)
    {
        printf("will take number\n");
    }
    else
    printf("will not take number\n");

    return 0;
}
