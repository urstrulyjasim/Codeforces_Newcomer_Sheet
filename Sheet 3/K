#include<stdio.h>
int main()
{
    long long N,sum=0;
    scanf("%lld",&N);
    long long a[N];
    for(int i=0;i<N;i++)
    scanf("%1lld",&a[i]);
    for(int i=0;i<N;i++)
    {
        sum += a[i];
    }
    printf("%lld\n",sum);
}
