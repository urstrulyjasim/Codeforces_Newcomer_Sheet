#include<stdio.h>
int main()
{
    int N,max;

    scanf("%d",&N);
    int a[N];
    for(int i=0;i<N;i++){
        scanf("%d",&a[i]);
    }
    max=a[0];
    for(int j=1;j<N;j++){
        if(a[j]>max){
            max=a[j];
        }
    }
    printf("%d\n",max);
    return 0;
}
