#include<stdio.h>
int main()
{
    int row,col,N;
    scanf("%d",&N);
    for(row=N;row>=1;row--){
        for(col=1;col<=row;col++){
            printf("*");
        }
        printf("\n");
    }
    return 0;
}
